
module.exports = function(api) {

    api.plugin('glutenLayoutMobile', function(api, data) {
        var result = {
            _ : '<!DOCTYPE html>'
            head
        };
        return result;
    });
}
